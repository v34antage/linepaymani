{
  "name": "linepay-order-server",
  "version": "1.0.0",
  "private": true,
  "description": "LINE Pay backend for online ordering.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  }
}
